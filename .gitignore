# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Per-deployment Compose settings (WEB_PORT); holds no secrets, but the port a
# given host publishes on is that host's business, not the repo's.
.env

# Obsolete since the SQLite migration; ignored so stale local copies stay invisible.
.env.keys
.env.docker

# Local SQLite database files (dev loop writes requesthole_backend/data/)
data/
*.db
*.db-wal
*.db-shm

# Branch-scoped task review scaffolding
reviews/
